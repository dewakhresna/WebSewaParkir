@{
    ViewBag.Title = "Dashboard Admin";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

@*@section styles {
        <link href="~/assets/css/custom-dashboard.css" rel="stylesheet" />
    }*@

<div class="pagetitle">
    <h1>Dashboard</h1>
    <nav>
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="">Home</a></li>
            <li class="breadcrumb-item active">Dashboard</li>
        </ol>
    </nav>
</div><!-- End Page Title -->

<section class="section dashboard">
    <div class="row">
        <!-- Recent Sales -->
        <div class="col-12">
            <div class="card recent-sales overflow-auto">


                <div class="card-body">
                    <h5 class="card-title">Data Transaction</h5>

                    <table class="table table-borderless datatable">
                        <thead>
                            <tr>
                                <th scope="col">No</th>
                                <th scope="col">Size Product</th>
                                <th scope="col">Customer Name</th>
                                <th scope="col">Payment Method</th>
                                <th scope="col">Payment Proof</th>
                                <th scope="col">Price</th>
                                <th scope="col">Payment Status</th>
                                <th scope="col">More</th>
                            </tr>
                        </thead>
                        <tbody>
                            <tr>
                                <th scope="row"><a href="#"></a>dsdsa</th>
                                <td>tes</td>
                                <td>tes</td>
                                <td>tes</td>
                                <td>tes</td>
                                <td>tes</td>
                                <td>tes</td>
                                <td>tes</td>
                                <td>tes</td>
                            </tr>
                        </tbody>
                    </table>
                </div>
            </div>
        </div><!-- End Recent Sales -->
    </div>
</section>

<!-- Basic Modal -->
<div class="modal fade" id="basicModal" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Payment Proof</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body text-center">
                <img id="payment-proof-img" src="" alt="Payment Proof" class="img-fluid mb-3">
            </div>
            <div class="modal-footer">
                <!-- Reject Form -->
                <form id="reject-form" method="POST" style="display:inline;">
                    <button type="submit" class="btn btn-danger">Reject</button>
                </form>

                <!-- Accept Form -->
                <form id="accept-form" method="POST" style="display:inline;">
                    <button type="submit" class="btn btn-success">Accept</button>
                </form>
            </div>
        </div>
    </div>
</div>

@*@section scripts {
        <script>
            console.log("Dashboard Admin Loaded!");
        </script>
    }*@